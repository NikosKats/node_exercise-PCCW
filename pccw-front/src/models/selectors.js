// state variables declaration

export const selectMessagesExchange = (state) => state.messagesExchange;

export const selectConversations = (state) => state.users;

// () => ({}) // instead of return use parenthesis
